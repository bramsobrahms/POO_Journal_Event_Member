plugins {
    id 'java'
}

group = 'dev.brahms'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation 'com.thoughtworks.xstream:xstream-distribution:1.4.20'
    implementation group: 'org.junit.jupiter', name: 'junit-jupiter-engine', version: '5.11.0-M1'
//    testImplementation platform('org.junit:junit-bom:5.10.0')
//    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}